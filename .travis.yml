language: python
python:
 - "2.7"
 
# Add these lines after the install section 

dist: trusty
sudo: required

addons:
 sonarcloud:
  organization: "team-ssw567-triangle567"
  token: "d90c2df0fa54e820b08148a70df96662408454ef"
  branches:
  - sonarcloud
 
# add the sonar-scanner command after all the other commands that you already have
script:
 - python -m unittest TestTriangle
 - sonar-scanner
